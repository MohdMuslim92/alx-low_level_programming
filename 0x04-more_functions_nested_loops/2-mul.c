#include "main.h"

/**
 * mul - Entry point
 * mul:multiply two numbers by each others
 * @a : parameter passed to the function
 * @b : parameter passed to the function
 * Return: Always 1 (number between 0 and 9)
 */
int mul(int a, int b)
{

		return (a * b);
}
