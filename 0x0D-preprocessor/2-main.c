#include <stdio.h>

/**
 * main - print file's name
 * Description: a program that prints the name,
 * followed by a new line
 * Return: return 0 (success)
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
