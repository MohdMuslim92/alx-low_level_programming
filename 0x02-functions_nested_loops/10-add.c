#include "main.h"

/**
 * add - Entry point
 * add : a function that adds two numbers to each other
 * @n : parameter passed to the function
 * @m : parameter passed to the function
 * Return: Always 0 (Success)
 */
int add(int n, int m)
{

	return (n + m);
}
